frm pyezviz import EzvizClient

EMAل حاب EZVIZ
PASSWORD = "كلمة السر"  # ← كلمة سر الحساب

try:
    client = EzvizClient(EMAIL, PASSWORD, "apiieu.ezvizlife.com")
    client.login()
    print("✅ تم الدخول!")
    
    devices = client.get_devices()
    print("📷 الأجهزة:")
    print(devices)
    
except Exception as e:
    print(f"❌ خطأ: {e}")
